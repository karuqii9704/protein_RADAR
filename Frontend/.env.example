# ========================================
# FRONTEND ENVIRONMENT CONFIGURATION
# ========================================

# Backend API URL - set this to your deployed Backend URL
# For Vercel deployment, set this in Vercel Dashboard > Project Settings > Environment Variables
# If Frontend and Backend are deployed separately:
NEXT_PUBLIC_API_URL="https://your-backend-domain.vercel.app"

# If Frontend and Backend are on the same domain (monorepo deployment):
# Leave this empty or don't set it - the app will use relative URLs automatically
# NEXT_PUBLIC_API_URL=""

# ========================================
# EXAMPLE CONFIGURATIONS
# ========================================

# Local Development (Backend running on port 3000)
# NEXT_PUBLIC_API_URL="http://localhost:3000"

# Production (Separate deployments)
# NEXT_PUBLIC_API_URL="https://dashboard-msu-backend.vercel.app"

# Production (Same domain - recommended)
# NEXT_PUBLIC_API_URL=""
